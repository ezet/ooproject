#include "stdafx.h"

// Below is an example of how one can explicitly specialize a functor template.
// However, we found it easier to implement them in the header file directly,
// which also is common practice. See functors.h for a better explanation.

/*
#include "utility/functors.h"
#include "entity\Player.h"

template struct NameEquals<Player>;

///
/// Name comparison functor
///
template <class T>
bool NameEquals<T>::operator() (const T& obj, const std::string& name) const {
    return obj.name() == name;
}
//*/